1) Jquery function allows us to grab the elements we put
into the parentheses, and jquery object represents a single DOM object or a set
of many DOM objects and an operation is applied on it then the operation will be 
applied to the DOM objects.

2) $("div h1").css("background-color", "yellow");

3) $("p").append("<strong> Yes! </strong>");

 