1) The javascript code will print out: 10, 8, 8, 9, 10, 1

2) A function is a block of code that performs a specific task, while 
method is a property containing a function definition.

3) 'this' in java method refers to the this specific instance of the class.

4) 'this' in javascript method refers to the object that owns the function.

5) 'this' in javascript constructor refers to the newly created object.

6) 'this' refers to the object x.

7) A free variable is a variable that a function inherits from it's surrounging
or variables that are not passed as an argument to the function or declared inside the function.

8) var student = {
	name: "fred",
	major: "music",
	min : function(x, y){
		if(x != y){
			if(x < y){
				return x;
			}
			else{
				return y;
			}
		}
		else{
			return x * y;
		}
	}
}

9) function employee(name, salary, position){
	this.name = name;
	this.salary = salary;
	this.position = position;
} 

const emp1 = new Employee("John", 200000, "Manager");
const emp2 = new Employee("Jane", 120000, "Developer");
const emp3 = new Employee("Doe", 100000, "HR");

10) var total = function(){
	let sum = 0;
	for(let i = 0; i < arguments.length; i++){
		sum += arguments[i];
	}
  
  return sum;
} 

11) var max = (x, y, z) => {
	let m = x;
  
  	if(y > m){
  		m = y;
  	}
  	if(z > m){
  		m =z;
  	}
  
  	return m;
}

